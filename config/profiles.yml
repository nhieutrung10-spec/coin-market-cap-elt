dbt_for_coin:
  outputs:
    dev:
      dbname: coin_analytics
      host: host.docker.internal
      pass: "{{ env_var('DB_PASSWORD') }}"
      port: 5432
      schema: analytics
      threads: 4
      type: postgres
      user: postgres
  target: dev